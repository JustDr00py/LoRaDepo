# ============================================================================
# LoRaDB UI Configuration
# ============================================================================

# Backend Configuration
BACKEND_PORT=3001

# LoRaDB API URL - Use the public IP/domain of your LoRaDB server
# Examples:
#   - http://192.168.1.100:8080 (local network)
#   - https://loradb.yourdomain.com (with reverse proxy)
#   - http://loradb-server.example.com:8080
LORADB_API_URL=http://YOUR_LORADB_SERVER_IP:8080

# JWT Configuration (must match LoRaDB server)
# SECURITY: Use a strong, random secret with at least 32 characters
# Include uppercase, lowercase, numbers, and special characters
# Generate with: openssl rand -base64 32
# Example: JWT_SECRET=Kx9#mP2$vL8@nQ4!wR7%tY6^uI3&oE5*
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_AT_LEAST_32_CHARS_LONG!!!
JWT_EXPIRATION_HOURS=1

# Frontend Configuration
FRONTEND_PORT=3000

# Frontend API URL - URL where the backend is accessible from your browser
# If accessing locally: http://localhost:3001
# If accessing remotely: http://YOUR_UI_SERVER_IP:3001
VITE_API_URL=http://localhost:3001

# CORS Configuration - Allow frontend origin
# SECURITY: In production, set this to your actual frontend URL(s)
# Supports comma-separated list for multiple origins
# Examples:
#   - http://localhost:3000 (development)
#   - https://loradb-ui.yourdomain.com (production)
#   - https://app1.example.com,https://app2.example.com (multiple)
CORS_ORIGIN=http://localhost:3000

# ============================================================================
# Production Security Checklist
# ============================================================================
# Before deploying to production, ensure:
# 1. JWT_SECRET is changed to a strong, random value (32+ characters)
# 2. LORADB_API_URL uses HTTPS in production
# 3. CORS_ORIGIN is set to your actual frontend domain(s)
# 4. VITE_API_URL points to your production backend URL
# 5. Firewall rules are configured to restrict access
# 6. SSL/TLS certificates are properly configured
# 7. Environment variables are not committed to version control
# 8. Regular security updates are scheduled
